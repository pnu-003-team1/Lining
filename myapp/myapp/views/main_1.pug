doctype html
head
  meta(charset='UTF-8')
  title fastrack
  link(rel='stylesheet', href='/stylesheets/full_style.css')

body
  input#email(type="hidden", name='email', value={email})
  input#full(type='checkbox', name='full', onclick='isfull(this)')
  label(for='full') 만석시 체크해주세요.

  section#res_off(data-role='page')
  .navbar
    a#a_selected(href='') 대기리스트
    a(href='/reservation') 오프라인 예약
    a(href='/mypage') 내 정보 관리

  script(src='http://code.jquery.com/jquery-1.10.2.js')
  script(type='text/javascript').
    function isfull(chkbox){
      var full = false;
      if(chkbox.checked == true){
        full = true;
      }
      else{
        full = false;
      }
      alert(full);

      var form = document.createElement("form");
      form.setAttribute("method", "post");
      form.setAttribute("action", "/main/fullCheck");

      var hiddenEmailField = document.createElement("input");
      hiddenEmailField.setAttribute("type", "hidden");
      hiddenEmailField.setAttribute("name", "email");
      hiddenEmailField.setAttribute("value", email);

      var hiddenFullField = document.createElement("input");
      hiddenFullField.setAttribute("type", "hidden");
      hiddenFullField.setAttribute("name", "full");
      hiddenFullField.setAttribute("value", full);

      form.appendChild(hiddenEmailField);
      form.appendChild(hiddenFullField);


      var url = "http://54.180.123.67:3000/buser/fullCheck";
      document.body.appendChild(form);
      form.submit();

    }
